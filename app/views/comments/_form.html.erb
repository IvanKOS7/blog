<%= form_with model: [post, Comment.new] do |form| %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content, size: "30x3" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
